const db = require('./databasecode.js');

console.log(`Welcome! You can use these for variables:
db.setVar('VariableName', 'VariableValue', 'UserID (or global)'); // Sets variable's value
db.fetchVar('VariableName'); // Gets variable's value
db.createVar('VariableName'); // Creates variable`);
